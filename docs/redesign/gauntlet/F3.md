# Gauntlet — F3 Widoki tablicy (2026-08-26)

Sześciu builderów na rozłącznych zestawach plików, potem trzej świeży krytycy na AK90–AK121,
poprawki, druga runda krytyka.

| Wynik | Checki |
|---|---|
| PASS w 1. rundzie (21) | AK90 AK91 AK92 AK93 AK94 AK95 AK96 AK98 AK99 AK100 AK101 AK102 AK103 AK104 AK106 AK107 AK108 AK109 AK110 AK112 AK113 AK117 AK120 |
| FAIL → naprawione (8) | AK105 (zapis 0,9–1,4 s), AK111/AK118/AK97 (stopki poza ekranem), AK115 (nie dało się dodać zadania), AK119 (wyciek milestone'ów + błąd przy tworzeniu widoku), AK114/AK116 (przesłonięte przyciski, niezwinięty pasek linków) |

## Błędy produkcyjne znalezione po drodze
1. `--color-fg-2` / `--color-fg-3` nigdy nie były zmapowane w `@theme` → 51 użyć `text-fg-2`/`text-fg-3` nie emitowało CSS.
2. Dialog milestone'u kasował opis przy każdym zapisie (montował edytor z `initial={null}`).
3. Zapis kanwy gubił `taskId`/`statusName`/`imagePath` i inne — karty zadań nie przeżywały reloadu.
4. Reguła „ukończone" w czterech rozjeżdżonych kopiach; ostatnia kolumna liczyła się zawsze.
5. Milestone z custom widoku ROADMAP wyciekał do domyślnej Roadmapy i Osi czasu.
6. Do Linii zadań nie dało się dodać zadania (`effectAllowed: "copy"` vs `dropEffect: "move"`).
7. Przenoszenie karty między etapami mogło ją skasować bezpowrotnie („usuń + dopnij").
8. Tworzenie custom widoku Whiteboard wywalało się na ekran błędu w ~2/3 prób (wyścig na `create`).
9. Zapis milestone'u czekał na pełną rundę do serwera przez tranzycję akcji formularza.
10. Przerwany łańcuch flex w `<main>`, layoucie przestrzeni i `BoardShell` — stopki uciekały poza ekran.

## Odstępstwa świadomie zaakceptowane
Pełna lista w `docs/redesign/OMITTED.md`. Najważniejsze: pasek „Linki tablicy" zwinięty zamiast
wyciętego, kolumna „Gotowe" nie jest zwinięta domyślnie mimo makiety, toolbar whiteboardu ma
„Zapisz" i „⋯" ponad makietę, limity WIP i zwinięcie kolumn kanbana w `localStorage`.

## Bramka
`tsc` 0 · `lint` 0 błędów (15 warningów) · `build` OK · Playwright **50/50** · 11 self-checków.
